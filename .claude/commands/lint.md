Run ESLint on the source files and report any issues.

```bash
npm run lint
```

If there are fixable issues, run:

```bash
./node_modules/.bin/eslint src --fix
```

This project uses React 19 class components with inline styles — no TypeScript, no CSS framework.
