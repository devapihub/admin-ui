Build the production bundle using Vite.

```bash
npm run build
```

Output goes to `dist/`. After building, you can preview with `npm run preview`.
For deployment, use `make deploy` to SCP the `dist/` folder to the remote server.
