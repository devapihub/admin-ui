Start the Vite development server on port 5173.

```bash
npm run dev
```

The dev server proxies `/api/*` requests to `http://localhost:8080` (local backend).
Make sure the backend is running on port 8080 before testing API calls.
